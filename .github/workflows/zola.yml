name: deploiement docusaurus github page 

on: [push, workflow_dispatch]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - name: test
          run: date

        - name: ls
          run: wget https://github.com/rust-lang/mdBook/releases/download/v0.4.36/mdbook-v0.4.36-aarch64-unknown-linux-musl.tar.gz

        - name: test
          run: tar -xzvf mdbook-v0.4.36-aarch64-unknown-linux-musl.tar.gz

        - name: test
          run: ls

 